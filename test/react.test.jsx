import { render } from '@testing-library/react'
import { describe, it } from 'vitest'

const Calculator = () => {
  
}

describe('Calculator', () => {
  it('Should render', () => {
    render(<Calculator />)
  })
})
