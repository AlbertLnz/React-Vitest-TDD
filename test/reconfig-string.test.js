import { describe, expect, it } from 'vitest'

const reconfigString = () => {
  
}

describe('reconfigString', () => {
  it('Should be a function', () => {
    expect(reconfigString).toBeTypeOf('function')
  })
})
